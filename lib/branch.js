// Generated by CoffeeScript 1.3.3
(function() {
  var Branch, async, _;

  async = require('async');

  _ = require('underscore');

  Branch = (function() {

    function Branch(store, head) {
      this.store = store;
      this.head = head;
    }

    Branch.prototype.commit = function(data) {
      return this.head = this.store.commit(this.head, data);
    };

    Branch.prototype.treeAtPath = function(path, cb) {
      return this.store.treeAtPath(this.head, path, cb);
    };

    Branch.prototype.dataAtPath = function(path) {
      return this.store.dataAtPath(this.head, path);
    };

    Branch.prototype.commonCommit = function(branch) {
      return this.store.commonCommit(this.head, branch.head);
    };

    Branch.prototype.diff = function(branch) {
      return this.store.diff(this.head, branch.head);
    };

    Branch.prototype.patchSince = function(trees) {
      return this.store.patchSince([this.head], trees);
    };

    Branch.prototype.merge = function(_arg, cb) {
      var branch, obj, strategy;
      branch = _arg.branch, strategy = _arg.strategy;
      obj = this;
      return this.store.merge(this.head, branch.head, strategy, function(err, head) {
        obj.head = head;
        return cb(null, head);
      });
    };

    return Branch;

  })();

  module.exports = Branch;

}).call(this);
